# Minification is disabled for this research prototype (see app/build.gradle),
# so no rules are strictly required. Kept as a placeholder for when the
# student enables minifyEnabled for a release build.
